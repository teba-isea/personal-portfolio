import styled from "@emotion/styled"

export const BioContainer = styled.div`
  display: flex;
  position: relative;
  top: -50px;
  background-color: var(--raisin-black);
  margin-top: -3rem;
  border-radius: 9rem 3px 3px 9rem;
  max-width: 640px;
  z-index: 999;

  p {
    margin: 0.8rem;
    padding: 0rem 2rem;
  }

  hr {
    width: 70%;
  }
`
